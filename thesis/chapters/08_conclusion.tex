% ============================================================
% 8.1 SUMMARY OF FINDINGS
% ------------------------------------------------------------
% Concise answer to each research question:
%
%   RQ1 (Can motifs be detected?): Yes -- SR is universal;
%   limit cycles rare but genuine in star; bistability present
%   on HiddenBench; dominance concentration significant in fc.
%
%   RQ2 (Under what conditions?): Topology is the dominant
%   structural axis. Star produces hub-driven limit cycles and
%   higher dominance; fc produces stronger SR. Memory window W
%   has modest secondary effects. Task difficulty (Tier 3) is
%   a prerequisite for all motif signals.
%
%   RQ3 (What do motifs reveal about outcomes?): SR drives
%   convergence speed universally (rho ~ -0.7); its accuracy
%   effect is dataset-dependent (positive on GPQA, absent on
%   HiddenBench). Dominance and bistability are informative
%   about the dynamical regime but do not dominate outcome
%   variance. Linguistic stylistic diversity within a run
%   predicts faster resolution.
%
% One paragraph per RQ, then a brief closing sentence on the
% broader contribution (motif detection as an explanatory lens
% for MAD, complementing outcome-focused evaluation).
% ============================================================
\subsection{Summary of Findings}

% [TO WRITE: ~0.5 page]


% ============================================================
% 8.2 FUTURE WORK
% ------------------------------------------------------------
% Prioritized list:
%   1. Fix SR null model (survivorship bias from downward
%      confidence drift censoring) -- already identified,
%      priority before final submission.
%   2. Causal perturbation experiments: seeded initialization
%      to certify basin geometry; asynchronous protocol variant
%      to isolate synchrony-induced cycles.
%   3. High-R single-task deep dive (NB 030 has the task
%      selection ready; experiment not yet run): 500-1000 reps
%      on one Tier-3 GPQA task for high-power motif analysis.
%   4. Extension to heterogeneous models: capability-asymmetry
%      effects on dominance (Wynn et al. 2025 framing).
%   5. Mixed-motive setting: LLM-Deliberation as a third track
%      for negotiation dynamics (flagged as future work in §4.3).
%   6. Independent linguistic scorer: replace Mistral judge
%      with a different model to eliminate same-model bias.
% ============================================================
\subsection{Future Work}

% [TO WRITE: ~0.5 page]
